---
# in this example we only allow https
lb_ingress_security_rules:
  - source: "{{ quad_zero_route }}"
    protocol: "{{ TCP_protocol }}"
    tcp_options:
      destination_port_range:
        min: {{ HTTPS_port }}
        max: {{ HTTPS_port }}

